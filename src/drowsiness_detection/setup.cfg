[develop]
script_dir=$base/lib/drowsiness_detection
[install]
install_scripts=$base/lib/drowsiness_detection

[build-system]
requires = ["setuptools", "wheel", "rosidl-setuptools"]
build-backend = "setuptools.build_meta"

[tool.rosidl]
generate_interfaces = true